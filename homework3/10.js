/* 10. Дан массив числами, например: [10, 20, 30, 50, 235, 3000]. 
Выведите на экран только те числа из массива, которые начинаются на цифру 1, 2 или 5.*/
var a = [10, 20, 30, 50, 235, 3000]
for (elem in a) {
    str = String(a[elem])
    if (str.slice(0,1) == "1" || str.slice(0,1) == "2" || str.slice(0,1) == "5") console.log(a[elem])
    else continue
}